/**
 * Metadata key for storing schema on a class
 */
export const SCHEMA_METADATA_KEY = 'standard-schema:schema';

/**
 * Metadata key for storing response schema on a method
 */
export const RESPONSE_SCHEMA_METADATA_KEY = 'standard-schema:response-schema';
